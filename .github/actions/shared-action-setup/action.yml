name: "Shared action - setup"
runs:
  using: "composite"
  steps:
    - name: Set up Node
      uses: actions/setup-node@v4
      with:
        node-version: 22

    - name: Install dependencies with Yarn
      shell: bash
      run: |
        corepack enable
        yarn install
        yarn after-install
